export default function Layout({children}) {
    return (
        <div className="bg-gradient-to-b min-h-screen from-gray-600 to-gray-900 text-white lg:px-10">
            {children}
        </div>
    )
}